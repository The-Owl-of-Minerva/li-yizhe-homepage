# 第一篇文章：博客框架示例

这是一个 **Markdown 驱动** 的文章模板。

以后写文章时，你只需要：

1. 在 `posts/` 文件夹新增一个 `.md` 文件；
2. 在 `posts/posts.json` 中登记标题、日期、摘要和文件路径；
3. `git add .`、`git commit`、`git push`；
4. Vercel 自动更新。

## 可以写什么

这里可以放研究感想、读书笔记、城市观察、技术笔记，或者更自由的长文。

> 这个示例只是框架。准备好第一篇正式文章后，可以直接删掉或替换它。
